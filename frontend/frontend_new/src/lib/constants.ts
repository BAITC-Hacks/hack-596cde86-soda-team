// Game rules from the spec (AGENTS.md §5–6), not dataset values.
export const SLOTS = 5;
export const MAX_PER_DIRECTION = 2;
export const CRITICAL_THRESHOLD = 40;
/** Budget bar turns yellow above this share of the budget. */
export const BUDGET_WARN_SHARE = 0.8;
